<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.csjava.campus.user.mapper.UserMapper">
    <select id="getPatriarchChild" parameterType="string" resultType="cn.csjava.campus.user.controller.vo.UserStudentResponse">
        SELECT * FROM student_apply WHERE user_id =#{userId}
    </select>
</mapper>